const initialState = {
  smurfs: [
    {
      id: 0,
      name: 'Brainey',
      age: 200,
      height: 5
    }
  ]
}

export default function reducer(state = initialState, action) {
  return state
}